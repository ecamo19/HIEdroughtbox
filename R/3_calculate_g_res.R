
residual_conductance <- function(initial_branch_weight_g,
                                 branch_surface_area_m2,
                                 strain_weight_g,
                                 total_leaf_area_m2,
                                 branch_dry_weight_g,
                                 gancho_weight_g = NULL,
                                 parafilm_weight_g = NULL
){

    # Billon et al 2020 formula -------------------------------------------------

    # First estimate transpiration (e: mmol s−1 m−2)
    #e <- -(slope/(total_leaf_area_m2 + branch_surface_area_m2))

    pass
}
